<%= form_for [@project, @user_story] do |f| %>
  <% if @user_story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_story.errors.count, "error") %> prohibited this user_story from being saved:</h2>

      <ul>
      <% @user_story.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :actor, "Como um:" %><br />
    <%= f.text_field :actor %>
  </div>
  <div class="field">
    <%= f.label :action, "Eu quero:" %><br />
    <%= f.text_area :action %>
  </div>
  <div class="field">
    <%= f.label :goal, "Para:" %><br />
    <%= f.text_area :goal %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
