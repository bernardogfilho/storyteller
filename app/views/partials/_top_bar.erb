<nav class="top-bar">
    <div class="wrapper">
      <%= link_to projects_path, :class => "logo-top" do %>
        <b>Story</b><i>Teller</i>
      <% end %>
      <% if user_signed_in? %>
      <ul class="top-bar__menu">
        <li>
        <%= link_to "Projects",  projects_path %>
        </li>
        <li>My Account</li>
        <li>Help</li>
      </ul>
      <ul class="top-bar__session">
        <li>Welcome, <%= current_user.email %></li>
        <li class="top-bar__session-links">
          <%= link_to '<i class="icon-cog"></i> Account Settings'.html_safe, edit_user_registration_path %>
        	<%= link_to '<i class="icon-signout"></i>Logout'.html_safe, destroy_user_session_path, :method => "delete" %>
        </li>
      </ul>
      <% else %>
      <ul class="nav-list--right">
        <li><a href="#">About</a></li>
        <li><a href="#">Help</a></li>
        <li><%= link_to "Sign Up", new_user_registration_path, :class => "label--dark" %></li>
      </ul>
      <% end %>
    </div>
</nav>